package com.brs.controller;

import java.util.List;

import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.annotation.EnableScheduling;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;
import org.springframework.stereotype.Service;

import com.brs.component.*;
import com.brs.controller.*;

public class RatingCalculateClient {
	
	/*public void updateDetails()
	{
		RatingCalculateController rcc=new RatingCalculateController();
		List<ReferenceMaster> rl=rcc.updateBuyerRatingInDatabase();
		for(ReferenceMaster r:rl)
			System.out.println(r);
		System.out.println("success");
	}*/
}
